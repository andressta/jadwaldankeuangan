# UI/UX Pro Max — installed skills

Vendored from [nextlevelbuilder/ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill) v2.13.0 (MIT license).

Skills: `banner-design`, `brand`, `design`, `design-system`, `slides`, `ui-styling`, `ui-ux-pro-max`.

To update to a newer version, re-copy the `.claude/skills/` folder from the upstream repo (or use their CLI: `npx ui-ux-pro-max-cli init --ai claude`), then diff before committing.
